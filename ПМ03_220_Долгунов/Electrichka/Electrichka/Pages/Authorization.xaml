<Page x:Class="Electrichka.Pages.Authorization"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Electrichka.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="350" d:DesignWidth="800"
      Title="Authorization">

    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Text="Добро Пожаловать" Height="80" Width="350" />
        <Button Grid.Row="2" Grid.Column="0" Height="80" Width="350" x:Name="RegistrarionBut" Click="RegistrarionBut_Click" Content="Регистрация"/>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Логин"  Height="80" Width="350" Margin="0 0 0 50"/>

        <TextBox Grid.Row="0" Grid.Column="1" x:Name="LoginBox" Height="80" Width="350" />
        <Button Grid.Row="2" Grid.Column="1" x:Name="AuthorizationButton" Click="AuthorizationButton_Click" Content="Войти в систему" Height="80" Width="350"   />
        
    </Grid>
</Page>
