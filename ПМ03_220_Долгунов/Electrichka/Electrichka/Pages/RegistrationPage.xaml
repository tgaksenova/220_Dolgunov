<Page x:Class="Electrichka.Pages.RegistrationPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Electrichka.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Registration">

    <Grid Background="White">
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>

        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="ФИО пользователь"  Height="80" Width="350" Margin="0 0 0 50"/>

        <TextBox Grid.Row="0" Grid.Column="1" x:Name="LoginBox" Height="80" Width="350" />
        <Button Grid.Row="3" Grid.Column="1" x:Name="RegistrationButton" Click="RegistrationButton_Click" Content="Зарегистрироваться" Height="80" Width="350"/>
    </Grid>
</Page>
